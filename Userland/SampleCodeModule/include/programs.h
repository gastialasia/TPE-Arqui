#ifndef _programs_
#define _programs_

char fibo(char*buff);
void reset_fibo();
char primos(char * buff);
void reset_primo();
char help(char*buff);
char date(char*buff);
char invalid(char*buff);
char infoRegisters();

#endif
